import Layout from '../components/UI/Layout';
import Users from '../components/Users/Users';

const Home: React.FC = () => {
  return (
    <Layout>
      <Users />
    </Layout>
  );
};

export default Home;
